import React, {Fragment} from "react";
import {LoginModal} from '../../modals/LoginModal/LoginModal';

export const Login = () => {
  return (
    <Fragment>
      <LoginModal/>
    </Fragment>
  );
};